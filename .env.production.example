# Production environment for Vercel / hosted deployments.
#
# Generate ENCRYPTION_KEY with: openssl rand -hex 32
ENCRYPTION_KEY=

# Optional: defaults if you don't configure per-row overrides
DEFAULT_REFRESH_SECONDS=60
STOCK_CACHE_TTL=60
